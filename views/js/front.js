document.addEventListener('DOMContentLoaded', (event) => {
    // Aquí iría la lógica relacionada con el módulo una vez que el DOM esté completamente cargado.
    console.log('El archivo front.js del módulo pvppriceswitcher se ha cargado correctamente.');
});